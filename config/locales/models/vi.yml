vi:
  activerecord:
    errors:
      models:
        product:
          attributes:
            images:
              image_format_invalid: "Không đúng định dạng hình ảnh"
              file_size_out_of_range: "Hình ảnh lớn hơn 5MB"
