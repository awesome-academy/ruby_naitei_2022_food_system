en:
  activerecord:
    errors:
      models:
        product:
          attributes:
            images:
              image_format_invalid: "must be a valid image format"
              file_size_out_of_range: "size %{file_size} should be less than 5MB"
